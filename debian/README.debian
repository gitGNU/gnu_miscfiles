Because Debian distributes the Jargon file in a separate package, we
don't install them from this package.  Also, we don't directly install
/usr/dict/words, though we provide web2 as an alternative for that.
